The file dedup.py in the current directory has a bug: dedup(xs) should return the list with duplicates removed while preserving first-seen order, but it sometimes misbehaves. Find and fix the bug so that dedup([3,1,3,2,1,2]) == [3,1,2]. Edit dedup.py in place, then verify your fix by running the file.
