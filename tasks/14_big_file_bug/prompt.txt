inventory.py in the current directory is a module with many small pricing/inventory helper functions. One of them, price_after_discount(subtotal), returns the wrong result at a tier boundary. The intended behaviour is: if subtotal >= 100 apply a 10% discount (return subtotal * 0.90); else if subtotal >= 50 apply a 5% discount (return subtotal * 0.95); otherwise return subtotal unchanged. Read the file, find the single buggy function, and fix it so the boundaries are correct. Do not change any other function.
