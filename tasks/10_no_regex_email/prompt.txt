Create validate.py in the current directory with a function is_valid_email(s) that returns True or False. Constraint: you may NOT import or use the re module (no regular expressions). A valid email must: contain exactly one "@"; have a non-empty part before the "@"; have at least one "." somewhere after the "@"; contain no spaces; and not start or end with "@" or ".". Then verify your solution runs.
