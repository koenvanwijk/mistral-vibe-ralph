The file app.py in the current directory defines two functions: slugify and greeting. Refactor it: move ONLY the slugify function into a new file utils.py, and change app.py to import slugify from utils (so that `import app; app.slugify("Hello World")` still returns "hello-world"). Do not change slugify's behavior. greeting stays in app.py. After refactoring, verify that importing app still works.
